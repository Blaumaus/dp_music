export const whitelist = ['en', 'uk', 'ru']

// temp
export const drawerWhitelist = ['home', 'settings', 'logout']

export default {
  TOKEN: 'TOKEN',
  JWT_TOKEN: 'jwtToken',
  APP_LANGUAGE: 'lang',
  THEME: 'theme',
}
